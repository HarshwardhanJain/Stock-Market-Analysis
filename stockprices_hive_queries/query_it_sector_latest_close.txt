WITH latest_date AS (
  SELECT MAX(date) AS max_date FROM stockprices
)
SELECT sp.symbol, sp.close, sc.sector, sp.date
FROM stockprices sp
JOIN stockcompanies sc ON sp.symbol = sc.ticker_symbol
JOIN latest_date ld ON sp.date = ld.max_date
WHERE sc.sector = 'Information Technology';
